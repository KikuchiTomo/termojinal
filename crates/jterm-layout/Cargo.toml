[package]
name = "jterm-layout"
version = "0.1.0"
edition.workspace = true
description = "Immutable split pane tree for jterm"

[dependencies]
log = "0.4"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
